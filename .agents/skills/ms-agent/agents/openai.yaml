interface:
  display_name: "Ms Agent"
  short_description: "Repo-specific patterns and workflows for ms-agent"
  default_prompt: "Use the ms-agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true